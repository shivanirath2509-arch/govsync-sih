import {Database} from 'lucide-react';
export default function Guide(){return <div className="panel"><div className="panel-body guide"><span className="badge">BUILD & UNDERSTAND</span><h2 style={{marginTop:15}}>The backend is already working.</h2><p>The interface calls server APIs and saves applications, consent, source records and audit events in Cloudflare D1. The department records are fictional. Real government integration requires authorized access.</p><div className="note"><Database size={20}/><span>React renders the interface. Server routes check permissions and process requests. D1 stores the results. Four sample department APIs expose deliberately different data formats.</span></div><h2>1. Follow a complete request</h2><ol><li>Select a sample applicant and grant four permissions.</li><li>POST /api/applications saves an application and 24-hour consent.</li><li>The browser calls Identity, Education, Income and Residence with the application reference.</li><li>Each API checks the signed-in owner, citizen match and consent.</li><li>POST /api/applications/:id/evaluate reads the saved records and checks demo eligibility on the server.</li><li>POST /api/applications/:id/submit validates the form and saves the submission.</li></ol><h2>2. Create a sample API</h2><p>A sample API is a real HTTP endpoint returning invented data. Fixtures live in <code>lib/demo.ts</code>. API handlers are in <code>app/api/[[...path]]/route.ts</code>.</p><pre className="code">{`// A fictional education response\n{\n  "student_name": "Asha Sharma",\n  "dob": "12/04/2006",\n  "percentage": 88.5,\n  "institution": "Demo Institute of Technology"\n}\n\n// Normalized by the server\n{\n  "name": "Asha Sharma",\n  "dateOfBirth": "2006-04-12",\n  "marks": 88.5\n}`}</pre><p>Use API explorer to send a request. To add a department, define its fictional data, add a route, map its fields, require its consent scope, and add it to the workflow. Return 404 for unknown citizens and 503 for simulated downtime.</p><h2>3. Run and learn locally</h2><p><a href="/backend-guide.md" download>Download the backend and sample API guide</a></p><p>The source package includes <code>BACKEND-GUIDE.md</code> with installation, database migration and local sign-in instructions. It also includes a standalone sample API server using Node's built-in HTTP module, so you can learn APIs without installing a framework.</p><p>This hosted version uses a Cloudflare-compatible server instead of an Express process. Request and response concepts are the same. PostgreSQL, Redis and AI are not required for this demonstration.</p><h2>4. Before connecting real departments</h2><ol><li>Obtain authorized API access and official documentation for each department.</li><li>Replace sample record functions with server-side API clients. Keep secrets out of the browser.</li><li>Connect the approved identity provider and enforce citizen, officer and auditor roles on every server request. This private prototype uses Sites sign-in; its navigation views are not government role permissions.</li><li>Confirm actual scholarship rules, identity matching and source conflict procedures.</li><li>Add durable background jobs, bounded retries, timeouts and officer exception handling. This prototype runs verification while the page is open and supports manual retry.</li><li>Define retention and deletion, consent renewal, encryption, backups and security testing before using personal data.</li><li>Add an approved notification provider. This prototype does not send email or SMS.</li></ol><h2>5. Demonstration scenarios</h2><p><strong>Asha Sharma:</strong> successful verification and submission.<br/><strong>Rohan Patil:</strong> income exceeds the demo limit.<br/><strong>Meera Joshi:</strong> source birth dates conflict.<br/><strong>Income outage:</strong> simulated 503, then recover and retry.<br/><strong>Revoked consent:</strong> further access returns 403.</p><p>No actual scholarship is awarded. Audit records are persistent but are not an immutable ledger.</p></div></div>}
